apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: cesieats-services
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: kilme/identity-service:$IMAGE_VERSION
          resources:
            limits:
              memory: "512Mi"
              cpu: "250m"
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: service-identity
  namespace: cesieats-services
spec:
  selector:
    app: identity-service
  ports:
    - name: http
      protocol: TCP
      port: 3000
      nodePort: 30101
  type: NodePort